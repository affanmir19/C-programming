#include <stdio.h>
int main()
{
  int i,n,j=0;
  printf("Enter the value of n :");
  scanf("%d",&n);
  for(i=1;i<=n;i++)
  {
    if(n%i==0)
    {
      j++;
    }
  }
  if(j==2)
  {
    printf("n is a prime number\n");
  }
  else
  {
    printf("n is a composite number\n");
  }
  return 0;
}
